/**
 * Official Massachusetts remaining table could not be fetched at compile time.
 * Desk fails closed — no placeholder counts.
 */
import type { Game } from "@/data/games";

export const MA_AS_OF = "unavailable";

export const MA_GAMES: Game[] = [];
