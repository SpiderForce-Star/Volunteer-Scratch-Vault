/**
 * Official Illinois remaining table could not be fetched at compile time.
 * Desk fails closed — no placeholder counts.
 */
import type { Game } from "@/data/games";

export const IL_AS_OF = "unavailable";

export const IL_GAMES: Game[] = [];
